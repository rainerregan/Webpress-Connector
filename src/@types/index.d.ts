interface UserProjectMetadata {
  name: string,
  id: string
}

interface UserData {
  userId: string,
}

interface ExportProjectData {
  projectId: string,
  userId: string,
  figmaUserId: string,
  htmlCss: string,
}